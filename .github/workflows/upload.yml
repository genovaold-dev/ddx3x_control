name: Upload to TestFlight

on:
  workflow_dispatch:

jobs:
  upload:
    runs-on: macos-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'

      - name: Install Fastlane
        run: gem install fastlane

      - name: Decode API key
        run: |
          echo "${{ secrets.APPSTORE_PRIVATE_KEY_BASE64 }}" | base64 --decode > AuthKey_6B92U7J5PV.p8

      - name: Create API key JSON
        run: |
          KEY_CONTENT=$(sed ':a;N;$!ba;s/\n/\\n/g' AuthKey_6B92U7J5PV.p8)
          echo "{
            \"key_id\": \"${{ secrets.APPSTORE_KEY_ID }}\",
            \"issuer_id\": \"${{ secrets.APPSTORE_ISSUER_ID }}\",
            \"key\": \"$KEY_CONTENT\"
          }" > api_key.json

      - name: Remove raw .p8 file so Fastlane cannot auto-load it
        run: |
          rm AuthKey_6B92U7J5PV.p8

      - name: Upload IPA to TestFlight
        run: |
          fastlane pilot upload \
            --ipa artifacts/ddx3x_control.ipa \
            --api_key_path api_key.json \
            --verbose
