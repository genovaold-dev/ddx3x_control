name: Upload to TestFlight

on:
  workflow_dispatch:

jobs:
  upload:
    runs-on: macos-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Remove ANY existing .p8 files
        run: |
          find . -name "*.p8" -type f -delete

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'

      - name: Install Fastlane
        run: gem install fastlane

      - name: Decode API key
        run: |
          echo "${{ secrets.APPSTORE_PRIVATE_KEY_BASE64 }}" | base64 --decode > private_key.p8

      - name: Create API key JSON
        run: |
          KEY_CONTENT=$(sed ':a;N;$!ba;s/\n/\\n/g' private_key.p8)
          echo "{
            \"key_id\": \"${{ secrets.APPSTORE_KEY_ID }}\",
            \"issuer_id\": \"${{ secrets.APPSTORE_ISSUER_ID }}\",
            \"key\": \"$KEY_CONTENT\"
          }" > api_key.json

      - name: Remove raw .p8 so Fastlane cannot auto-load it
        run: |
          rm private_key.p8

      - name: Upload IPA to TestFlight
        run: |
          fastlane pilot upload \
            --ipa artifacts/ddx3x_control.ipa \
            --api_key_path api_key.json \
            --verbose
